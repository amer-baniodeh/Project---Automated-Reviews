# models/B_clustering/

- `kmeans_model.pkl` — fitted KMeans model (generated by `src/clustering/cluster.py::fit_clusters`,
  not committed to git — regenerate locally or download from your model registry)
- `cluster_labels.json` — human-readable labels assigned after inspecting cluster contents
  (see `notebooks/B_clustering/01_baseline_KMeans.ipynb`). Edit this file after re-running clustering.
